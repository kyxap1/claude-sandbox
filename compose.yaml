services:
  claude:
    build:
      context: .devcontainer
      args:
        NODE_VERSION:
        CLAUDE_CODE_VERSION:
        GIT_DELTA_VERSION:
    cap_add:
      - NET_ADMIN
      - NET_RAW
    volumes:
      - .:/workspace
